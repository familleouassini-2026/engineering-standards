# Safe placeholders only. Never commit real credentials.

# APP_ENV=development
# PUBLIC_APP_URL=http://localhost:3000

# Database / backend public endpoint
# PUBLIC_BACKEND_URL=

# Public client key if the platform explicitly intends it to be public
# PUBLIC_CLIENT_KEY=

# Server-only secrets are configured in local secret storage / deployment platform.
# SERVER_SECRET=
